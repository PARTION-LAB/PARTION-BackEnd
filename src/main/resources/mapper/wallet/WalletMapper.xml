<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.partion.wallet.mapper.WalletMapper">

    <insert id="insert" parameterType="com.partion.wallet.domain.Wallet"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wallets (
        member_id,
        available_balance,
        locked_balance
        )
        VALUES (
        #{memberId},
        #{availableBalance},
        #{lockedBalance}
        )
    </insert>

    <select id="findByMemberId" parameterType="long" resultType="com.partion.wallet.domain.Wallet">
        SELECT
        id,
        member_id,
        available_balance,
        locked_balance,
        created_at,
        updated_at
        FROM wallets
        WHERE member_id = #{memberId}
    </select>

    <select id="findById" parameterType="long" resultType="com.partion.wallet.domain.Wallet">
        SELECT
        id,
        member_id,
        available_balance,
        locked_balance,
        created_at,
        updated_at
        FROM wallets
        WHERE id = #{id}
    </select>

    <update id="increaseAvailableBalance">
        UPDATE wallets
        SET
        available_balance = available_balance + #{amount}
        WHERE id = #{id}
    </update>

</mapper>